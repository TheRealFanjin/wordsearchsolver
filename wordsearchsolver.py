print('Type the letters here, pressing enter after each row. Type . (period) when done')
while True:
    a = input()
    if a == '.':
        break
listOfLetters = [[None] * width] * height
def checkHorizontal(self):
    for row in listOfLetters:
       try:
            indexPos = listOfLetters[row].index(str(word)).rindex(str(word))
       except:
           continue
       return indexPos